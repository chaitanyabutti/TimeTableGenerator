
// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyA2B7Sglnav-0kEdlyceujZ52I2TVYf2n4',
    authDomain: 'time-table-scheduler.firebaseapp.com',
    databaseURL: 'https://time-table-scheduler.firebaseio.com',
    projectId: 'time-table-scheduler',
    storageBucket: '',
    messagingSenderId: '586416410278',
    appId: '1:586416410278:web:50f75e9fdb8c0d0a'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
