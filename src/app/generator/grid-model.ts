export class Grid {
    // metaData: boolean;
    // textContent: string;
    // textColor: string;
    // gridSpan: number;
    // gridColor: string;
    // rowPos: number;
    // colPos: number;
    // dayIndex: number;
    // gdColumn: string;
    constructor(
        public metaData: boolean,
        public textContent: string,
        public textColor: string,
        public gridSpan: number,
        public gridColor: string,
        public rowPos: number,
        public colPos: number,
        public dayIndex: number,
        public gdColumn: string,
    ) {
    }
}
